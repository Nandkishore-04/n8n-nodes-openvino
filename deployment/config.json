{
  "model_config_list": [
    {
      "config": {
        "name": "text-classifier",
        "base_path": "/models/text-classifier",
        "target_device": "AUTO",
        "plugin_config": { "PERFORMANCE_HINT": "LATENCY" }
      }
    },
    {
      "config": {
        "name": "text-sr",
        "base_path": "/models/text-sr",
        "target_device": "AUTO",
        "plugin_config": { "PERFORMANCE_HINT": "LATENCY" }
      }
    }
  ]
}
